import React from "react";
import "../styles/buttons.css";

const Buttons = () => {
  return (
    <div className="buttons">
      <button>All Pictures</button>
      <button>Liked Pictures</button>
    </div>
  );
};

export default Buttons;
